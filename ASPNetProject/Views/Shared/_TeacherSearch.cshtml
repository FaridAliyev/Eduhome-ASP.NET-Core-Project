@model IEnumerable<Teacher>

@foreach (Teacher item in Model)
{
    <li class="list-group-item" style="padding-bottom:13px">
        <a asp-controller="Teacher" asp-action="Details" asp-route-id="@item.Id">@item.Fullname</a>
    </li>
}
